services:
  - type: web
    name: zeelandcamper-site
    runtime: node
    plan: free
    buildCommand: npm ci
    startCommand: npm start
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: rc-autoverkoop
          property: connectionString
      - key: SITE_USERNAME
        value: bram
      - key: SITE_PASSWORD
        value: "1234"
      - key: SESSION_SECRET
        generateValue: true
      - key: MOBILOX_EMAIL
        sync: false
      - key: MOBILOX_PASSWORD
        sync: false
